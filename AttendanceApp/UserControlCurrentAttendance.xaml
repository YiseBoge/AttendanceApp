<UserControl x:Class="AttendanceApp.UserControlCurrentAttendance"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AttendanceApp"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch" 
             VerticalAlignment="Stretch">

    <Grid MinHeight="400" MinWidth="400">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>


        <DataGrid AlternatingRowBackground="Gray" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Width="*"/>
                <DataGridTextColumn Header="Student CourseName" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="1" Grid.Column="1" Width="100" Content="Save" FontFamily="Champagne &amp; Limousines" FontSize="16"/>

    </Grid>
</UserControl>
