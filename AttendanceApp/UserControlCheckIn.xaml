<UserControl x:Class="AttendanceApp.UserControlCheckIn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:AttendanceApp"
             mc:Ignorable="d" 
             >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="67*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="13*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <Image x:Name="LiveView" Grid.Column="0" Grid.Row="0" Margin="5" Source="Assets/home.png"/>

        <TextBlock Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Bottom" FontSize="25" Padding="10" Text="Check In with credentials"/>
        <Grid  Grid.Row="3" HorizontalAlignment="Stretch" Margin="150,10" MaxWidth="600">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="StudentIdField" Grid.Column="0" Margin="0,10" materialDesign:HintAssist.Hint="Student ID" Style="{StaticResource MaterialDesignFloatingHintTextBox}" FontFamily="Champagne &amp; Limousines" FontSize="16"/>
            <PasswordBox Grid.Column="1" Padding="5,0" x:Name="StudentPasswordField" Margin="0,10" materialDesign:HintAssist.Hint="Student Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" FontFamily="Champagne &amp; Limousines" FontSize="16"/>
        </Grid>

        <Button x:Name="CheckInBtn" Grid.Row="4" Width="200" Margin="15"  Content="Check In" FontFamily="Champagne &amp; Limousines" FontSize="16" Click="CheckInBtn_Click"/>

    </Grid>
</UserControl>
